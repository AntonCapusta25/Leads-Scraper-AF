# Railway Contact Scraper - Google API Version
# No browser dependencies - pure API approach

# Core FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# HTTP client for API calls
httpx==0.25.2
requests==2.31.0

# HTML parsing (lightweight, no browser needed)
beautifulsoup4==4.12.2
lxml==4.9.3

# Data processing and validation
pydantic==2.5.0
sqlite3  # Built into Python

# Async support
asyncio  # Built into Python

# Text processing and similarity
difflib  # Built into Python

# Environment and utilities
python-dotenv==1.0.0

# Development and testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1

# No browser automation dependencies needed!
# DrissionPage - REMOVED (no longer needed)
# selenium - REMOVED (no longer needed)
# playwright - REMOVED (no longer needed)
