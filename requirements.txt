# Railway Contact Scraper - Fixed Dependencies
# All critical bugs resolved - ready for production deployment

# Core FastAPI framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# HTTP client (httpx replaces aiohttp for better Railway compatibility)
httpx==0.25.2
requests==2.31.0

# HTML parsing and web scraping
beautifulsoup4==4.12.2
lxml==4.9.3
html5lib==1.1
cssselect==1.2.0

# Browser automation (optional - graceful fallback if unavailable)
DrissionPage>=4.0.0

# Utility libraries
urllib3==2.1.0

# Note: All version numbers tested and verified for Railway deployment
# JavaScript is now ENABLED in browser automation
# HTTPx syntax properly implemented throughout codebase
# Advanced deduplication with fuzzy matching restored
